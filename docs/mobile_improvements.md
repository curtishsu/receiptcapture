mobile_improvements.md

1. Latency: opening the app is extremely slow
2. The top card title is way too big. Make it significantly smaller. It doesn't need its own section. It should jsut be the name of the app as the header, and the icon for the account settings. 
3. Change the name of the app to foodprint
4. There is too much subtext throughout the app. Remove it. Key points to remove are
4a. On the photo page, the only text is "Upload a grocery receipt photo" Remove all other subtext
4b. Remove 'Saved receipts' section on Receipt history
4c. Remove text on Item mappings