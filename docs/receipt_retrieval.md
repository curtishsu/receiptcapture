improvements.md

1. The name of each item shows up twice. Should only show the line item you expect
2. For the quantity, the increments should be 0.5
3. Purchase date should be inferred from the receipt photo. If you cannot tell, default the date to the date the photo was uploaded
4. Remove the 'Using Claude' text
5. The table formatting is incorrect. The column headers do not align with the columns
6. The unit be an enum, in which it is a drop down. The drop down should be (alphabetically) all the past enums. At the bottom of the list is 'Add' and if the user clicks it, then they can insert via text a new unit